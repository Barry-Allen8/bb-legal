{
  "name": "bb-legal",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "lint": "node scripts/audit.mjs",
    "type-check": "node --check build.mjs && node --check site-data.mjs && node --check script.js && node --check scripts/audit.mjs",
    "test": "node scripts/audit.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
